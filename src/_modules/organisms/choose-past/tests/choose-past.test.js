'use strict';

import ChoosePast from '../choose-past';

describe('ChoosePast View', function() {

  beforeEach(() => {
    this.choosePast = new ChoosePast();
  });

  it('Should run a few assertions', () => {
    expect(this.choosePast);
  });

});
